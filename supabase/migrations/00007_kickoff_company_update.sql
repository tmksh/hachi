-- ============================================================
-- エイトデザイン株式会社 キックオフ情報反映
-- 2026-05-06 記入内容を本番テナントへ適用
-- Supabase SQL Editor で実行してください。
-- ============================================================

-- 会社情報の更新
UPDATE companies
SET
  name       = 'エイトデザイン株式会社',
  settings   = settings || jsonb_build_object(
    'representative',   '上坪文哉',
    'address',          '愛知県名古屋市昭和区鶴舞2丁目16-5',
    'invoice_number',   'T-9180001073069',
    -- メール設定
    'mail_services',    ARRAY['Gmail', 'Xserver'],
    'mail_auth_methods', ARRAY['OAuth', 'IMAP'],
    -- 勤怠設定
    'clock_methods',    ARRAY['PC', 'スマホ'],
    'existing_attendance_system', '連携なし',
    -- ワークフロー設定
    'workflow_approval_routes', '稟議（申請者→部門長→経理→代表）
経費精算（申請者→上司）
有給（申請者→直属上司）
金額10万以上：代表承認必要',
    'digital_seal_required', true
  ),
  updated_at = NOW()
WHERE id = '00000000-0000-0000-0000-000000000001';

-- 部署マスタを反映（profiles の department を実態に合わせてサンプル更新）
-- 実際の社員招待時はこの部署名を使用してください。
-- 部署一覧: アドバイザー（営業）/ ディレクター（施工管理）/ デザイナー（設計）/
--           プレス（広報）/ グラフィック / 企画室 / バックオフィス

-- オーナーのプロフィールを代表者情報に合わせて更新
UPDATE profiles
SET
  display_name = '上坪 文哉',
  department   = '企画室',
  position     = '代表取締役',
  updated_at   = NOW()
WHERE id = '00000000-0000-0000-0000-000000000010';

-- ============================================================
-- 備考・要対応事項（キックオフヒアリングより）
-- ============================================================
-- 【ワークフロー】
--   社内稟議書が70種以上存在するため、申請種別・タイトル・
--   承認フロー・記入項目を管理者が自由に設定できるテンプレート
--   機能の実装が必要。電子印影は「必要」と回答済み。
--   → workflow_templates テーブルの追加を検討。
--
-- 【勤怠】
--   始業・終業・法定休日・指定休日などは管理者が画面から
--   設定できるようにしてほしい。
--   打刻方法: PC・スマホ（GPS・ICカードは不要）
--   既存勤怠システム: 連携なし（KING OF TIME 等への移行不要）
--   → company settings の work_hours / holidays キーを活用、
--     または attendance_settings テーブルを追加。
--
-- 【メール】
--   使用サービス: Gmail ＋ Xserver
--   連携方式: OAuth（推奨）＋ IMAP 対応
--   署名テンプレートは各ユーザーが自由に設定できるようにしてほしい。
--   → profiles.settings JSONB に email_signature キーを追加予定。
-- ============================================================
